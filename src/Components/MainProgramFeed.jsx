import React from 'react'

const MainProgramFeed = () => {
  const[programs, setPrograms] = useState([])

  const fetchPrograms = async () => {
    const response = await Programse.index()
  }
  return (
    <div>
      
    </div>
  )
}

export default MainProgramFeed
